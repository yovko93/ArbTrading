# Phase 04A.1 verification — monitoring CSV portability

Published Phase 04A baseline: `d60e9a819392bc4af9e68d9a7cd0aeafd9612a32` on `main`, starting from a clean working tree. GitHub Actions reported Windows success and Linux backend failure: `MonitoringStorageTests.Csv_is_invariant_atomic_bounded_rotated_and_failure_isolated` expected `Failed` but received `Healthy` at line 66. The Linux build had zero warnings/errors; Domain 55, Application 204 and 257 other Backend integration tests passed.

The former test opened `current-opportunities.csv` with `FileShare.None`, then assumed the production `File.Move(temp, target, overwrite: true)` must fail. Windows enforces that sharing restriction for replacement. Unix generally permits replacing the directory entry while an existing target file is open, so a healthy Linux write is valid behavior.

Changed files: `tests/Arbitrage.Backend.IntegrationTests/MonitoringStorageTests.cs` and this verification note. The main regression now deletes the previously written target inside its isolated temporary directory and creates a directory at that exact target path. `SnapshotAsync` still runs its real writer: the incompatible destination deterministically causes export failure, yielding `Failed`, `CsvWriteUnavailable` and one incremented failure without incrementing snapshots. Removing the directory and writing again restores `Healthy`, clears the error, and leaves an atomic CSV with no pending file. The old `FileShare.None` check remains separately inside an `OperatingSystem.IsWindows()` condition and recovers afterward. This does not ask Unix to reject a legal rename.

Repository search found no other test requiring an open handle to prevent cross-platform replacement. Other `FileShare.None` uses protect creation, ownership or Windows-specific credential handling; none showed this test assumption. Production `MonitoringCsv`, atomic flush/rename, row bounds, alert rotation, CSV injection/link protections and failure isolation are unchanged. All Phase 04A paper accounting, UI, migrations and execution behavior are unchanged. No tests or CI jobs were disabled.

The original test passed on Windows before the change, consistent with its Windows-only assumption. The corrected focused test also passed on Windows. WSL Ubuntu is installed and the checkout is mounted, but `dotnet` is not installed in that distro; `bash scripts/verify-backend.sh` therefore cannot execute there. No local Linux/.NET test success is claimed. A GitHub Linux CI rerun remains pending; this agent did not trigger or mutate Actions.

Local Windows verification: `dotnet restore ArbitrageTrading.sln` passed. `dotnet build ArbitrageTrading.sln -c Release --no-restore` passed with zero warnings and errors. The focused corrected test passed (1/1). `dotnet test ArbitrageTrading.sln -c Release --no-build --no-restore` passed all 531 tests: Domain 55, Application 204, Backend integration 258, Desktop 14. `./scripts/verify.ps1` independently passed its Release build (zero warnings/errors) and all 531 tests. `dotnet ef migrations has-pending-model-changes --project src/Arbitrage.Infrastructure --configuration Release --no-build` reported no pending changes. `dotnet list ArbitrageTrading.sln package --vulnerable --include-transitive` reported no vulnerable packages across the 13 projects. `git diff --check` passed.

Changes remain uncommitted; there was no push or Phase 04B work.
