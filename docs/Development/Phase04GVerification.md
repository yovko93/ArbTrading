# Phase 04G verification — paper burn-in and reliability evidence

Verification date: 2026-09-24. Isolated paper fixtures only; no production burn-in was run.

## A–D. Repository and baseline

- **A:** Root `C:\Users\Yovko\source\repos\ArbTrading`, ordinary checkout on `main`; starting HEAD `260224ce94393b1e12161275d11635effad541ff` (`Add deterministic adaptive paper sizing`), parent `7ef069d6820d10c93f17bb2714924fcfd73de91a`. Initial tree clean. Root AGENTS.md applied. Existing origin retained without printing its URL or credentials.
- **B:** Before implementation, exact-HEAD GitHub Actions run [35972856040](https://github.com/yovko93/ArbTrading/actions/runs/35972856040) was completed/success. This is baseline CI evidence, not CI for the present uncommitted changes.
- **C:** Existing solution, branch, history, financial semantics, prior migrations and unrelated files preserved. No reset/clean/stash/branch switch/remote modification. Ignore rules extended for local campaign exports; build, SQLite/sidecars, credentials and logs remain excluded. Tracked filename inspection found no database/private key/credential files.
- **D:** Added reliability policy/telemetry, campaign store/evaluator/entities/migration, backend coordinator/endpoints, Contracts/client/desktop page/VM, policy/integration/persistence/desktop/WPF tests. Extended observer hooks, durable writer anchors, read-only reconciliation mode, navigation/SignalR registration and EF snapshot. Updated the shell navigation assertion for the new page; made the existing retained-opportunity policy fixture use a fixed clock after observing book expiry under full-suite load. Updated README, paper architecture, roadmap, Execution README and this report. No dependency added. Changed-file inventory: 41 files (22 modified, 19 new): Backend 7; Infrastructure/migrations 11; Execution 2; Contracts 1; Desktop 7; tests/project links 8; documentation and ignore rules 5.

## E–U. Implementation

| Item | Evidence/design |
|---|---|
| E — Lifecycle | Owner-only start/pause/resume/evaluate/complete/cancel/export. One active collecting/paused campaign per workspace; revision guards, bounded history; completed/cancelled snapshot frozen. No startup campaign. |
| F — Policy | Standard version 1: backend 24h, monitoring 24h, armed 4h, healthy 4h, inputs 100, distinct inputs 25, sizing attempts 20, automatic executions 5, fully settled minimum 0; unexpected worker faults maximum 0; every mandatory invariant satisfied. Inclusive thresholds, no single progress percentage. |
| G — Runtime | TimeProvider integer ticks, overlapping observed states; healthy requires armed + monitoring Running + successful safety validation. Profile/risk/generation updates invalidate health. Pause and downtime excluded. These measure locally observed states, not external venue availability. |
| H — Persistence | Aggregate ephemeral counters, distinct hashed triggers, contiguous backend collection intervals; 30-second checkpoints and five-minute local evaluations. Events/intervals/references bounded at 10,000; evaluations at 100; no tick or sizing-candidate rows. |
| I — Gaps | Counter/retention/scan limits, clock regression, uncertain shutdown, concurrent financial revision changes and telemetry failures block a pass. Collector failures are isolated; monitoring/automation continue under their existing gates. Failed final evaluation reopens collection with a gap instead of getting stuck. Recovery persists gaps. |
| J — Invariants | Cash, duplicate request/session-input identities, explicit arm, writer-ordered kill, historical risk/automation/fees/trust/strategy/quality/sizing/grid/quantity/session-debit evidence, open/settled state, duplicate payout, exact ManualScenario payout and ledger reconciliation. |
| K — Evaluation | Scoped local durable facts, bounded scans, captured historical policy/proofs, exact decimal arithmetic; no current-market reevaluation of historical fills. Missing/truncated evidence cannot pass. Proven violations remain sticky. |
| L — Reconciliation | Existing reconciler reused with readOnly=true. It does not mark/repair financial rows. Relevant campaign generations plus current generation; financial revision changes during sampling produce unavailable evidence. |
| M — Funnel | Dequeued key/stamp offers count CandidateInputsObserved; duplicate/coalesced monitoring stages are separately named. Outer outcome aggregates use Funnel: keys. Inner adaptive candidate rejects and retryable budget deferrals are not disjoint terminal totals. |
| N — Sizing | Separate fixed/adaptive entries and proof validity; adaptive quantity/candidate min/max/decimal mean. 5,10,15 => min 5/max 15/mean 10; .1,.2,.3 => mean .2. No recommendation generated. |
| O — Risk | Historical proof validation binds approved decision, plan, quantity, revision and captured arm risk policy; valid/invalid counts. |
| P — Kill | Latch/reset and execution anchors share serialized writer ordering; equal timestamps do not determine ordering. Counts, last-latch UTC ticks and violation count exposed. |
| Q — Integrity | Existing reconciler plus independent negative cash/identity/payout checks. Campaign actions and export do not change financial rows. Sticky violations prevent reuse of implicated generations without explicit reset. |
| R — Restart | Collecting resumes with a new backend interval. Clean shutdown flushes observed time; unclosed prior intervals stop at their checkpoint and mark a gap. Auto Paper remains disarmed. |
| S — Economics | Automatic campaign entries only; generation/venue/currency grouping. Settlement evidence excludes paused/outside intervals. Expected basket payout allocated equally between legs and labeled as an allocation; actual payout uses ManualScenario. No USD/USDC sum. |
| T — State | Proven violation takes precedence. Gap/Unknown/insufficient minimums => InsufficientEvidence. Complete minimums with failed maximum => CriteriaNotMet. Complete thresholds and satisfied invariants => CriteriaMet. Lifecycle stays independent. |
| U — Report | Canonical sorted evidence SHA-256, unsupported-policy Unknown handling, persisted evaluation time, explicit simulation disclaimer and limitations. Controlled private backend `reliability/<workspace>/<campaign>/report.json`, 10 MB limit, link rejection, flushed temporary file and atomic replacement. Re-export is byte stable. |

## V–Z. Deterministic and preservation evidence

- **V:** Fake-clock policy fixture: 2h observed + 1h downtime + 2h observed = exactly 4h backend runtime; armed runtime remains 2h after restart. Separate 10h pause contributes zero. HTTP lifecycle fixture also proves 2h + 10h pause + 2h = 4h. Inclusive criteria, one-tick-short, 4-versus-5 executions, zero-versus-one faults and Unknown/sticky precedence tested. Aggregate funnel fixture has 100 offers: 70 duplicates, 10 quality, 5 cooldown, 5 no-size, 5 risk, 5 committed; 31 exact distinct stamps. This fixture tests aggregation, not fabricated production market activity.
- **W:** Real SQLite tampering covers negative cash, missing risk, off-grid sizing, unresolved fees, manual relationship, orphan arm, mismatched backend identity and kill-before-execution; repeat evaluation retains violation. Settlement duplicate-credit fixture and duplicate request/session-input fixtures directly check durable evidence. A valid execution-before-latch fixture uses identical UTC timestamps. A paused-settlement fixture also uses equal timestamps and proves writer-order exclusion. Existing baseline execution tests continue to exercise authoritative kill and financial races.
- **X:** Financial serialization before/after start/pause/resume/evaluate/complete/export is identical. Fixed/adaptive and tampered financial snapshots remain unchanged by evaluation. Campaign completion leaves automation armed; later executions do not change its frozen report.
- **Y:** Reliability has no connector, exchange HTTP, account, signing or acquisition dependency. Backend fixtures use the existing isolated in-memory sources; WPF fixtures throw on any attempted network request. Report reads use local persisted data only.
- **Z:** Exact Phase 04F schema (`20260923191645_PaperAutomation`; 04F had no schema change) upgraded with populated adaptive execution/proof/financial rows. Serialization of every preexisting financial row is unchanged. New campaign tables begin empty. Existing historical upgrade fixtures also run in the complete suite.

## AA–AK. Verification and limits

- **AA:** `scripts/verify.ps1` passed: restore, Release build (0 warnings / 0 errors), and 811 tests passed, 0 failed, 0 skipped. Domain 55; Application 277; Backend integration 459; Desktop 20. This adds 37 cases over the 774-test Phase 04F baseline. Final full run used process-local `DOTNET_PROCESSOR_COUNT=4` to bound test parallelism. Focused reliability integration: 31 passed. Earlier full runs identified an outdated navigation count and a real-clock book-expiry fixture; both were corrected without weakening assertions. `git diff --check` passed. The final presentation-only duration-format change was followed by another successful solution Release build (0 warnings/errors) and the WPF fixture test (1 passed, all 18 theme/state renders, exact readable-duration assertions).
- **AB:** `dotnet ef migrations has-pending-model-changes --project src/Arbitrage.Infrastructure --configuration Release --no-build`: no pending model changes.
- **AC:** `dotnet list ArbitrageTrading.sln package --vulnerable --include-transitive`: all 13 projects report no vulnerable packages from current sources; no dependency changed.
- **AD:** Enumerated installed WSL distributions and checked Ubuntu with `command -v dotnet`: no Linux SDK executable on PATH or in the standard per-user SDK directory. Linux backend verification was not run; nothing installed. Windows verification is not represented as Linux evidence.
- **AE:** Nine WPF scenarios in Light/Dark (18 renders): no campaign, collecting, paused, insufficient, criteria met, criteria not met, invariant violation, gap, completed. Render fixture passes with network forbidden. Representative final Light and Dark views visually inspected; explicit theme background and economics column spacing corrected. Desktop tests cover access/navigation/invalidation/selection stale responses and 401/403/409 one-attempt command failure. No unattended production campaign was started.
- **AF:** Reports are evidence snapshots, not live financial authority. Observed healthy duration is bounded by local state checks. Abrupt loss can discard uncheckpointed ephemeral counts; the recovered campaign is gapped. Scan/retention limits intentionally prevent claiming full evidence. The 100-offer funnel is an aggregate fixture; the 10,001-trigger and 10,000-event tests exercise bounds, not sustained production throughput. Expected per-venue payout is a displayed allocation. No multi-day production burn-in, actual exchange fills/latency or Linux SDK execution is claimed.
- **AG:** CriteriaMet never enables live execution or changes admission/arming.
- **AH:** No production synthetic burn-in generator added.
- **AI:** No new paper/live execution capability, alternate sizing algorithm or changed risk/kill gate added.
- **AJ:** No exchange account or credential required or collected.
- **AK:** Changes remain uncommitted; no push, merge or PR. Existing checkout remains on main.

Suggested commit message: `Add paper reliability campaigns and reports`
