# Public exchange integration

## Phase 03A settlement relationship boundary

Reviewed 2026-09-22: [Polymarket markets/events](https://docs.polymarket.com/concepts/markets-events), [resolution](https://docs.polymarket.com/concepts/resolution), [negative risk](https://docs.polymarket.com/concepts/negative-risk), [market details](https://docs.polymarket.com/market-data/market-details); Kalshi [market details](https://docs.kalshi.com/api-reference/market/get-market), [events](https://docs.kalshi.com/api-reference/events/get-event), [series](https://docs.kalshi.com/api-reference/market/get-series), [multivariate events](https://docs.kalshi.com/api-reference/events/get-multivariate-events), [lifecycle](https://docs.kalshi.com/getting_started/market_lifecycle), and [settlement](https://docs.kalshi.com/getting_started/market_settlement). The complete URL inventory, design and actual verification are in [Phase03AVerification](Phase03AVerification.md).

Catalog `SourceReference` is a public market reference, not an authoritative settlement source. Relationship descriptors retain it separately. Explicit selected-pair enrichment uses Gamma `/markets/{id}` or Kalshi `/trade-api/v2/markets/{ticker}`, `/events/{event_ticker}`, `/series/{series_ticker}`. At most two markets are enriched per action (one to three public reads per market), with response/time/concurrency bounds, no credentials or linked-document fetching. Polymarket description/rules/resolutionSource and outcome/token arrays are retained; Kalshi primary/secondary rules, settlement_sources, contract links, strikes, early-close and multivariate metadata remain inspectable. Cached enrichment is tied to the underlying catalog fingerprint, and changed outcomes invalidate approvals. Navigation, header refresh, startup, sync and candidate generation do not initiate enrichment.

Native group membership, negative risk, a shared series or similar timestamps are candidate evidence only. Augmented negative-risk placeholders and Other definitions can change; a retrieved subset of an event must never become automatically exhaustive. Market close, occurrence, expected expiration, source update and retrieval are separate concepts. Unknown timezone or settlement qualifiers remain blockers.

The Domain relationship validator requires evidenced subject/predicate, observation boundaries, threshold/operator/unit, event edition/stage, geography, authority, rules and outcome meanings. Title-derived hints are advisory. It separately models exclusivity and exhaustiveness; mappings use native token/side identities, including inverse YES→NO meaning. SHA-256 semantic fingerprints and policy version 1 enforce Stale on material source/policy changes. Manual review is owner-authorized, confirmed and audited, with independent manual trust. The strategy provider excludes all unapproved/stale relationships and requires explicit opt-in for manual approvals. No account, AI, price comparison, arbitrage or orderbook subscription is added by relationship matching.

## Phase 02A public catalog

Verified against official documentation on 2026-09-22:

- [Polymarket Discover Markets](https://docs.polymarket.com/market-data/discover-markets) and [Market Details](https://docs.polymarket.com/market-data/market-details).
- [Kalshi Market Data Quick Start](https://docs.kalshi.com/getting_started/quick_start_market_data) and [Get Markets](https://docs.kalshi.com/api-reference/market/get-markets).

The catalog adapters use fixed production HTTPS hosts and dedicated unauthenticated clients. No local bearer credential, exchange account credential, proxy rotation, or arbitrary URL fetch is involved. The desktop talks only to its authenticated local backend. Phase 02B.1 adds separate public orderbook clients, described below.

Polymarket uses `GET https://gamma-api.polymarket.com/markets/keyset?closed=false&limit=100`; each `next_cursor` is passed unchanged as `after_cursor`. This covers publicly enumerable markets whose Gamma `closed` filter is false, without selecting a tag/category or excluding negative-risk markets. The raw Gamma envelope is `{ markets: [...], next_cursor: ... }`. Raw `outcomes` and `clobTokenIds` are JSON-encoded arrays; the adapter parses both and correlates by index only when lengths match. This differs from SDK-normalized `outcomes.yes`/`outcomes.no` objects. Missing tags, event references, and token arrays remain missing with warnings. `active` and `acceptingOrders` are retained as native state; normalized labels are conservative and do not claim trading readiness. `startDate`, `endDate`, `closedTime`, and `updatedAt` remain distinct.

Kalshi uses `GET https://external-api.kalshi.com/trade-api/v2/markets?status=<scope>&limit=200`, traversing `unopened`, `open`, `paused`, and `closed` separately. A returned opaque `cursor` is passed unchanged as `cursor`. The public endpoint is unauthenticated. The raw envelope is `{ markets: [...], cursor: ... }`. Native `ticker`, `event_ticker`, `mve_collection_ticker`, `market_type`, `status`, `rules_primary`, `rules_secondary`, and source timestamps are preserved where returned. The listing does not provide series category or tags, so those remain unknown. Kalshi outcome labels Yes/No have no fabricated token IDs. No `mve_filter=exclude` is applied, preserving listed multivariate markets. No incompatible `min_updated_ts` filter is used.

The Kalshi quick start mentions `status=all`, while the endpoint reference lists only `unopened`, `open`, `paused`, `closed`, and `settled` and says an empty status returns any state. The adapter uses the four explicit non-finalized filters instead of relying on the quick-start shorthand or paging through historical settled markets.

For both exchanges, a page's continuation cursor controls traversal even when the page is short. A repeated cursor or several non-progressing pages fails the run. Records are keyed by exchange plus native ID, stored per page, and never deleted due to a partial or failed run. Complete means reaching the end of every selected scope during this run, not an atomic global exchange snapshot. Historical settled backfill and efficient incremental synchronization are deferred.

The backend allows one page request per exchange at a time, paces requests by at least 300 ms by default, bounds each request to 12 seconds and 8 MB, retries safe transient GET failures at most twice, and honors `Retry-After`. `Local:PolymarketRequestIntervalMs` and `Local:KalshiRequestIntervalMs` configure pacing independently from 100–10,000 ms. Default requested page size is 200 (effective Polymarket maximum 100, Kalshi 200) and default run budget is 15 minutes; `Local:DiscoveryPageSize` accepts 1–500 and `Local:DiscoveryRunMinutes` accepts 1–120. Hitting the time budget or a `Retry-After` beyond it yields Partial. A 401/403 public response fails only that exchange run and does not revoke local workspace access. Malformed envelopes fail; malformed records without usable identity are counted. External text and references are displayed as inert text and never fetched automatically. Prices are omitted because listing data is not an executable quote.

Optional sampled live connectivity check, from the repository root:

```powershell
./scripts/smoke-market-discovery.ps1 -Live
```

This makes at most two read-only requests per exchange with a page size of five, following one continuation only when returned. It neither starts a backend nor writes normal user storage; the C# adapter sample verifies parsed records in isolated temporary SQLite and does **not** verify full traversal. To exercise an installation, launch backend and desktop with the isolated paths described in `LocalDevelopment.md`, then explicitly select **Sync Markets**. The full run can be long and should not be used as an automated smoke test.

The original Phase 02A **shell-only** sample returned five Kalshi entries. Polymarket did not produce a parsed response; a separate status-only shell check reported an untrusted TLS certificate chain (`SEC_E_UNTRUSTED_ROOT`). Certificate verification was not bypassed. Those observations do not prove the C# adapter path. See `Phase02A.1Verification.md` for the newer C# sample.

## Phase 02A.1 contract correction

[Kalshi's lifecycle documentation](https://docs.kalshi.com/getting_started/market_lifecycle) separates request filter names from REST response statuses: `unopened` selects `initialized`, `open` selects `active`, `paused` selects `inactive`, and `closed` selects markets past close that are not finalized. The adapter maps `initialized` → Upcoming, `active` → Open, `inactive` → Paused, `closed` → Closed, `determined` → Determined, `disputed` → Disputed, `amended` → Amended, and `finalized` → Finalized. Future native values stay Unknown with a warning. Polymarket `closed=true` means trading has closed; it is labeled Closed, never inferred to be finalized. Neither exchange's timestamps prove settlement.

The [Kalshi Get Markets contract](https://docs.kalshi.com/api-reference/market/get-markets) requires a string cursor; the empty string ends paging. The Phase 02A.1 parser accepted a string, null, or omission only with explicit `has_more=false`; Phase 02A.2 replaces that behavior with the dedicated keyset API contract. Unsupported cursor types fail the run. Short pages do not imply termination. The four Kalshi buckets and the `closed=false` Polymarket feed are changing upstream data, never an atomic snapshot. Prior Kalshi runs covering only three buckets do not satisfy current full-coverage metadata.

Test JSON is contract-shaped synthetic data based on those official pages, not a live recording. The opt-in script now calls production C# adapters and stores each sampled page in its own temporary SQLite database. It now issues at most two GETs per exchange with a page size of five and labels the result Sampled. A successful page does not mean Complete coverage. Secure-connection failures stop without ordinary transient retries; the reported classification does not assert a specific certificate-chain cause unless separately observed.

## Phase 02A.2 Polymarket keyset correction

The [official keyset API reference](https://docs.polymarket.com/api-reference/markets/list-markets-keyset-pagination) and [May 14, 2026 changelog](https://docs.polymarket.com/changelog/predictions) set `GET /markets/keyset` to `1 <= limit <= 100`. `Local:DiscoveryPageSize` remains a requested size of 1–500. The Polymarket adapter uses `min(requested, 100)` for **every** outbound keyset request; Kalshi continues to receive the requested size. Cursor traversal continues until the documented terminal response, so a smaller effective page does not impose a total-record cutoff.

The keyset reference says `next_cursor` is omitted on the last page. A valid `markets` array with an omitted cursor ends traversal, whether it contains records or is empty. A present nonempty string is passed unchanged as `after_cursor` even on a short page. Present null, empty string, boolean, number, array, and object cursor values are rejected because this endpoint's reference documents an opaque string when continuation exists and omission at termination. Kalshi's required string cursor and empty-string terminal rule remain separate. The synthetic parser and SQLite tests verify these rules; they are not live recordings.

Production and the optional C# sample share `PublicMarketTransport.CreateHandler()` with redirects and proxy use disabled. Normal certificate and hostname checks remain enabled. The sample makes one attempt per page, follows at most one cursor, caps each page at five records, reports each stage and exchange separately, and exits nonzero if either requested check fails. A parsed empty page is reported as `SampledEmptyParsed`, not evidence of market-record validation. See `Phase02A.2Verification.md` for the actual live result and remaining environment limitation.

## Phase 02B.1 REST orderbooks

Official sources checked **2026-09-22**, starting with [Polymarket's index](https://docs.polymarket.com/llms.txt) and [Kalshi's index](https://docs.kalshi.com/llms.txt):

- [Polymarket Prices and Order Books](https://docs.polymarket.com/market-data/prices-order-books), [Real-Time Data](https://docs.polymarket.com/market-data/realtime-data), and [Predictions changelog](https://docs.polymarket.com/changelog/predictions).
- [Kalshi Orderbook Responses](https://docs.kalshi.com/getting_started/orderbook_responses), [Fixed-Point Representation](https://docs.kalshi.com/getting_started/fixed_point_migration), [Get Market Orderbook](https://docs.kalshi.com/api-reference/market/get-market-orderbook), [Get Multiple Market Orderbooks](https://docs.kalshi.com/api-reference/market/get-multiple-market-orderbooks), [API Environments](https://docs.kalshi.com/getting_started/api_environments), [Rate Limits](https://docs.kalshi.com/getting_started/rate_limits), and [Get Markets](https://docs.kalshi.com/api-reference/market/get-markets).

The linked [CLOB OpenAPI](https://docs.polymarket.com/api-spec/clob-openapi.yaml) could not be retrieved: the research reader failed and the direct local HTTPS read failed certificate name/chain validation. No trust setting was changed. Polymarket fixtures therefore follow the explicit **raw REST** examples on the official Prices and Order Books page, not SDK field names. This is a documented verification limitation.

| Exchange | Implemented GET | Wire data | Interpretation |
| --- | --- | --- | --- |
| Polymarket | `https://clob.polymarket.com/book?token_id=<catalog token>` | `asset_id`, `market` (condition identity), millisecond `timestamp`, `hash`, `bids`/`asks` with string `price`/`size` | Native bid/ask levels for the selected outcome token. Gamma market ID remains separate from condition ID. Returned asset must match requested token. |
| Kalshi | `https://external-api.kalshi.com/trade-api/v2/markets/<catalog ticker>/orderbook?depth=0` | `orderbook_fp.yes_dollars` and `.no_dollars`; pairs of string dollar price and fixed-point quantity | Native YES/NO bids. No source asks or timestamp are invented. `depth=0` requests all levels. |

Polymarket's single-book endpoint documents no depth parameter. Its batch endpoint accepts up to 500 tokens; this implementation deliberately fetches one selected token. Kalshi documents zero depth as all levels and positive depth 1–100 as limited depth. Its multi-book endpoint is not used. Documentation differs on array order; normalization always sorts independently. Kalshi's generated endpoint reference lists authentication headers, while its Orderbook Responses guide explicitly says no authentication is required. The production adapter sends none; the live sample confirmed HTTP 200 without credentials. A future denial is `PublicAccessDenied`, never a trigger to collect credentials. Rate tiers are exchange-specific; this application does not assume an authenticated quota. It serializes manual refreshes, spaces connector requests by at least 300 ms, and respects Retry-After.

### Canonical data and calculations

`OrderBookInstrumentId` includes exchange, catalog-native market ID, native instrument ID, and outcome label. For Polymarket, only stored aligned outcome/token pairs with decimal-digit token identifiers are addressable; missing or duplicate token mappings cannot be inferred. For Kalshi, `yes`/`no` are side identifiers scoped to the ticker, not invented token IDs. Only catalog classification `binary` plus complete Yes/No outcome metadata permits complements. Scalar, unknown and existing `Multivariate` classifications retain native bids and opposite bids, but have no executable asks. Existing catalog classification does not retain the underlying binary type for MVE; support is deliberately conservative until that metadata is available.

An immutable snapshot retains full returned levels (maximum 20,000 per side), retrieval UTC, separate optional source time/hash, native source market identity, validity, warnings and completeness (`FullReturnedDepth`). This means all levels of that bounded REST response, not global market consistency or a sequence-safe feed. Financial strings parse directly to decimal with invariant culture, no exponent/locale/NaN conversion or floating point. Oversized or excessive-precision strings are rejected rather than rounded. Kalshi accepts price scale up to four places and quantity scale up to two; these are current fixed-point fields, not integer-cent fallback. Canonical prices use payout units in [0,1]. Per-market tick-grid validation, minimum trade sizing and order eligibility are not asserted by this read-only L2 estimator.

The single normalizer sorts bids descending and asks ascending, removes zero-size levels, and rejects negative sizes, illegal prices and side/provenance contradictions. Duplicate prices invalidate the snapshot: an L2 entry may already be an aggregated total, so summing repeated entries could double liquidity. Crossed books are invalid; locked books are conservatively invalid because matching/atomicity guarantees have not been established. Empty sides are valid and have no best price. Kalshi validates both native bid sides before deriving `1 - opposite bid price` with unchanged quantity; each derived ask is `DerivedComplement`. Native own-side and opposite-side bids remain reconstructible. A later loss of binary catalog metadata suppresses cached complements immediately.

`ExecutableDepth.Calculate` walks full asks for Buy and full bids for Sell. It reports requested/executable/unfilled quantity, gross notional, quantity-weighted VWAP over actual depth, best/worst consumed price, level count, snapshot ID/time, freshness, actionability and reason. Quantity must be positive. Full and partial depth are explicit; no-liquidity prices and VWAP are null. Default stale or failed-refresh results calculate zero executable quantity. Explicit diagnostic mode may inspect stale valid data but is never actionable; invalid/unsupported data cannot be calculated even diagnostically. Decimal overflow rejects the preview. No fees, minimum order compliance, payout, edge, profit, ROI, execution or strategy results are calculated.

### Cache, APIs and desktop

The backend singleton `OrderBookCache` is thread-safe and bounded to 128 instruments by default (`Local:OrderBookCacheCapacity`, range 1–1024). It evicts the oldest updated entry. Only the catalog service admits API refreshes; arbitrary unknown identifiers cannot create cache entries. Cached GETs do not allocate entries. Immutable snapshots replace atomically, older retrievals cannot overwrite newer books, invalid/failed refreshes preserve the prior accepted snapshot with separate failure time/code/retry metadata. Restart creates an empty cache; SQLite catalog storage is unchanged and no migration/history table is added.

`Local:OrderBookFreshnessSeconds` defaults to 5 (validated 1–60). `TimeProvider` computes local retrieval age; backward/future clock ages are non-actionable. This is a conservative manual diagnostic threshold, not a trading-quality claim. Source timestamps and catalog retrieval age remain separate; fetching a book never refreshes catalog metadata. `BookEligibility` is the shared actionability predicate and latest refresh failures disable it.

Authenticated workspace routes under `/api/v1/workspaces/{workspaceId}/orderbooks/{exchange}/{marketId}`:

- `GET ?instrumentId=...`: cached state plus addressable instruments; omission selects the first stored addressable outcome.
- `POST /refresh?instrumentId=...`: one explicit selected-instrument public GET with safe retries. Global admission allows one refresh at a time, returns 409 when busy, and has no unbounded queue.
- `POST /depth` with `{ "instrumentId": "yes", "action": "Buy", "quantity": 25, "diagnosticOnly": false }`: cached gross estimate, no implicit refresh.

Every route checks authenticated workspace membership before catalog lookup/cache access. Unknown catalog market is 404; mismatched instrument or invalid input is 400. No token is accepted without a known catalog mapping. Read/depth paths make no external requests. Unsupported/not-addressable, validity, freshness, last refresh failure, and retained prior snapshot are distinct response concepts.

Dedicated public HttpClients use the existing handler with redirects/proxies disabled and ordinary certificate/hostname validation. Timeout is 12 seconds per HTTP operation within a 15-second source budget, payload is capped at 2 MB both by declared length and streamed bytes, and at most two safe GET attempts are made. Retry-After beyond the small local retry allowance is returned as a failure with retry time. Errors distinguish access denial, rate limits, timeout, secure connection, transport, JSON/envelope, malformed book, and unsupported structure. Clients bearing auth headers are rejected before sending. No raw payload or credential is logged.

Market Explorer displays an outcome selector, explicit Refresh Order Book, local state/age/source/retrieval timestamps, BBO/spread, native/derived labels and first ten levels per side. Full depth stays available to the backend calculator. Phase 02B.2 adds a read-only gross-depth entry UI backed by the same authenticated preview API. No navigation, catalog notification or header Refresh initiates public orderbook traffic. A local display timer updates age; Phase 02B.2 also coalesces realtime cache reads to one per second. Selection, access generation, workspace, backend instance and active state guard results; cancellation/disposal settle Loading and prevent late restoration. Backend instance changes clear old snapshots. Cancellation reaches the service/source safely because these are bounded read-only requests, not accepted long-lived jobs.

### Bounded live verification and future boundary

Run `./scripts/smoke-market-discovery.ps1 -Live -OrderBooks` explicitly. Default test runs remain offline. The orderbook mode uses one catalog GET for five open standard markets (`mve_filter=exclude`) and one book GET, one attempt each. This optional discovery filter applies only to the sample; normal catalog synchronization still includes multivariate markets. The first development sample of 20 unfiltered records contained no supported binary metadata and made no book request; the corrected bounded sample succeeded at **2026-09-22T11:18:02Z**. See the verification report for exact levels and result.

Polymarket is `NotAttemptedBecauseCatalogInstrumentUnavailable`: Gamma is network-restricted and this isolated sample has no safely acquired current token. Fixtures verify the CLOB contract; live compatibility is unverified. No stale hardcoded token, DNS override, VPN/proxy/relay, insecure certificate or TLS bypass was used.

Phase 02B.2 extends this same canonical model/cache with the realtime subsystem below. REST remains snapshot data with no continuity guarantee. Order submission, paper fills, scanning and arbitrage detection remain absent.


## Phase 02B.2 — explicit realtime market data

Verified **2026-09-22**, before implementation, using official documentation:

| Source | Verified contract |
|---|---|
| [Polymarket index](https://docs.polymarket.com/llms.txt), [AsyncAPI](https://docs.polymarket.com/asyncapi.json) | `wss://ws-subscriptions-clob.polymarket.com/ws/market`; public market channel; `assets_ids`, `type: market`, `initial_dump: true`, `level: 2`; incremental unsubscribe uses `operation: unsubscribe`. Text `PING` every ten seconds, text `PONG` response. |
| [Polymarket realtime](https://docs.polymarket.com/market-data/realtime-data), [prices/books](https://docs.polymarket.com/market-data/prices-order-books) | Full `book`; `price_change.price_changes[]` carries absolute aggregate size, BUY bids/SELL asks, zero removes. Instrument identity is asset/token ID, not Gamma ID. No strict sequence field. |
| [Polymarket changelog](https://docs.polymarket.com/changelog/predictions) | Tick increments are not limited to powers of ten; .0025 is supported. `tick_size_change` updates future-delta validation without reinterpreting existing levels. |
| [Kalshi index](https://docs.kalshi.com/llms.txt), [environments](https://docs.kalshi.com/getting_started/api_environments), [WebSocket quick start](https://docs.kalshi.com/getting_started/quick_start_websockets) | Production `wss://external-api-ws.kalshi.com/trade-api/ws/v2`; demo `wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2` (documented only, not selectable). Authentication required at handshake, including market data. |
| [Kalshi API keys](https://docs.kalshi.com/getting_started/api_keys) | KALSHI-ACCESS-KEY, KALSHI-ACCESS-TIMESTAMP, KALSHI-ACCESS-SIGNATURE. Decimal Unix milliseconds + uppercase GET + `/trade-api/ws/v2`, UTF-8, RSA-PSS SHA256/MGF1-SHA256, digest-length salt, standard base64. |
| [Kalshi connection](https://docs.kalshi.com/websockets/websocket-connection), [AsyncAPI YAML](https://docs.kalshi.com/asyncapi.yaml) | `cmd: subscribe`, channel `orderbook_delta`, `market_tickers`; bind acknowledged `sid`; subscription-scoped `seq`, including sequenced control responses. Unsubscribe with `params.sids`. Schema documents limit/rate errors 26/27 without a usable numeric market cap. |
| [Kalshi orderbooks](https://docs.kalshi.com/websockets/orderbook-updates), [fixed point](https://docs.kalshi.com/getting_started/fixed_point_migration) | WS snapshot fields are `yes_dollars_fp` / `no_dollars_fp` (different from REST); signed deltas use `price_dollars`, `delta_fp`, `side`, ticker/market identity, optional source time. Four price decimals, two quantity decimals. |
| [Kalshi keepalive](https://docs.kalshi.com/websockets/connection-keep-alive), [rate limits](https://docs.kalshi.com/getting_started/rate_limits) | Server control Ping every ten seconds; ClientWebSocket handles Pong during receive. No invented text-PONG command or account-dependent rate entitlement. |

There is no official deterministic signing vector in the reviewed sources. RSA-PSS signatures are randomized: tests verify the exact input and algorithm with an ephemeral generated RSA key rather than requiring identical signature bytes. The integration uses current dedicated Kalshi WS hosts, not older shared-host examples. Upstream numeric WebSocket market/command limits were not published in the reviewed schemas; the application limit is explicitly its own conservative bound.

### Ownership, state, and resource bounds

`RealtimeOrderBookManager` is the backend owner. One receive task/socket per exchange serves a bounded desired set; no WPF-owned external sockets. A single Kalshi channel subscription carries all selected tickers with one SID/sequence tracker; YES/NO views share native market events when both are requested. Polymarket assets remain separately identified. Start/Stop are catalog-admitted workspace-authorized POSTs under the existing book route: `/realtime/start` and `/realtime/stop`, with `instrumentId`. Repeated Start is idempotent during an active run; explicit Start after a terminal failure begins a new bounded attempt series. Unsupported Kalshi structures are rejected. Subscription ownership does not cross workspace boundaries.

Changing the desired set cancels the exchange generation, immediately invalidates remaining anchors, closes the old socket, then subscribes the new bounded set. Remaining instruments reconnect and reanchor. This intentionally trades a short interruption of that exchange for an unambiguous sequence boundary; the other exchange is unaffected. Protocol faults conservatively invalidate the shared connection scope. Stopped books remain visible but non-actionable. Credentials changing stops all Kalshi subscriptions and clears their desired set; recovery requires explicit Start. Backend shutdown cancels both owners and awaits bounded close.

Bounds: `Local:RealtimeMaximumInstruments` defaults 16, range 1–32, further limited by cache capacity; no full-catalog subscription. Frame cap 2,000,000 bytes, receive buffer 8,192 bytes, JSON depth 32, Poly batch cap 256, canonical side cap 20,000 levels. Socket frames are consumed serially with no internal frame channel or durable stream. Per-message staged dictionaries permit atomic validation/publication, and published snapshots are immutable. Retry budget is five connection attempts per explicit run; exponential delay starts 500 ms with 0–250 ms jitter, capped at 16 s; connect budget 15 s, receive budget 35 s, close budget 2 s. Auth failure, missing credentials and secure/network restriction terminate retries immediately. No TLS, DNS, trust-store or proxy bypass exists.

### Reconciliation and cache priority

Deltas never anchor from REST or an older generation. Full snapshots validate all native levels and establish a current anchor. Kalshi binds SID and ticker/market identity, advances the shared subscription sequence exactly once for every sequenced frame, rejects duplicate/backward/skipped sequence, and adds signed deltas exactly. Zero removes; negative results fail instead of clamping. YES/NO native bids pass through the same `NormalizeBinary` used by REST, which derives asks as decimal `1 - opposite bid` and retains provenance. A gap invalidates immediately, preserves the last visible snapshot, and requires a fresh generation/anchor. The seq100→101 +5→102 -15→104 missing103 case is tested.

Polymarket full books replace native bids/asks, changes replace absolute quantities, and multi-asset changes are validated before committing any affected book. Asset identity and the anchored condition ID must match. Later tick changes constrain new delta prices. Its public feed supplies no sequence suitable for strict gap detection: **BestEffort is the strongest available guarantee**, never Continuous. Old generation frames are ignored. Protocol uncertainty triggers reconnect/reanchor. This limitation must remain visible to future strategy consumers.

The existing bounded cache contains a REST diagnostic lane and a realtime current lane per instrument, not two independent caches. Starting/reconnecting invalidates actionability immediately; an old REST/book may remain visible while awaiting an anchor. Accepted realtime snapshots have priority while subscribed. Explicit REST refresh updates only the diagnostic REST lane and never changes realtime sequence/continuity. Stop retains the last realtime view non-actionably; a subsequent explicit REST refresh selects REST again. Active realtime entries are protected from ordinary REST eviction. Failure metadata is retained separately. No per-delta database writes or migrations.

REST freshness stays five seconds. `Local:RealtimeFreshnessSeconds` defaults ten seconds (1–60), computed through TimeProvider from the last book publication, not socket/pong activity. Anchor time, generation/session/SID, sequence, source time, last received/control/book-change times, tick size and local cache version remain distinct. Streaming requires current anchor, canonical validity, supported structure, acceptable continuity and age. Kalshi requires Continuous; Polymarket permits explicitly labeled BestEffort. The existing depth calculator selects the required side, calculates exact gross depth and reports empty/partial liquidity. Realtime diagnostic requests cannot bypass unresolved continuity or freshness failures. Old depth UI estimates expire on book replacement or stale age.

### Desktop notifications and credentials

A shared 250 ms scheduler coalesces each active instrument's version/status into tiny workspace-scoped SignalR invalidations. The existing outbound channel remains bounded at 256 with nonblocking TryWrite. No full book or secret is sent through SignalR. A slow desktop cannot stall socket ingestion. WPF reads the selected cached book at most once per second while realtime is displayed, providing recovery from dropped invalidations. Navigating away cancels page reads/timers but leaves backend subscriptions intact. Mutation results and events retain workspace/backend/access/selection-generation guards; mutations never replay after 401.

Settings provides key ID, file selection, import/replace/remove, safe status/fingerprint and last authentication result. No PEM is held in view-model fields. The authenticated local owner alone may administer credentials; workspace membership is insufficient. Replacement/removal require explicit confirmation and an expected credential version. Files must be absolute, bounded regular owner-private files with no reparse/link ancestry. Accepted unencrypted RSA private PEM forms are PKCS#1 and PKCS#8, 2048–4096 bits, validated by export and PSS signing self-test. Import leaves the original file unchanged.

`IExchangeCredentialStore` has no Windows API dependency. Infrastructure implements Windows CurrentUser DPAPI with protected owner-only directories/files, flushed temporary writes and atomic replacement. Stored at `Local:DataDirectory/credentials/kalshi.dpapi`, outside Git and separate from SQLite. Byte buffers and short-lived signing leases are zeroed; each handshake creates/disposes its own RSA instance. Safe metadata only crosses the API. No exchange private key/signature/signing input/blob enters DTOs, diagnostics, preferences, appsettings or command lines. Removal deletes the protected file and prevents future leases. Last authentication result is process-local and resets after restart. Linux production storage reports UnsupportedPlatform; offline tests use fake stores. Future Server mode needs an authenticated HTTPS enrollment service or server secret manager, not arbitrary local path import.

The opt-in `scripts/sample-kalshi-realtime.ps1` reads only explicitly selected protected local runtime metadata, checks credential status, reuses one open binary catalog market and samples for 5–30 seconds (default20), then stops in finally. It refuses an already active selected subscription, emits sanitized summary fields, and runs the existing gross-depth API if actionable liquidity exists. It does not import/search for exchange credentials. Absence returns NotConfigured without external traffic. Polymarket live testing remains NotAttemptedInstrumentUnavailable; no safe current token was supplied, and the known ISP/TLS restrictions were not bypassed. These live limitations are separate from passing production-parser replay tests.
